version: '3.8'

services:
  devsecopsapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devsecopsapp
    ports:
      - "8081:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
    networks:
      - devsecops-network

networks:
  devsecops-network:
    driver: bridge
    